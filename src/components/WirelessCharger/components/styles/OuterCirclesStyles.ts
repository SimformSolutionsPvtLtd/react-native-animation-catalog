import { StyleSheet } from 'react-native';

const styles = () =>
  StyleSheet.create({
    outerCircleStyle: {
      justifyContent: 'center',
      alignItems: 'center',
    },
  });

export default styles;
